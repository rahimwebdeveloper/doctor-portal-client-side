import React from 'react';

const ButtonPrimary = ({children}) => {
    return (
        <button className="btn text-white bg-gradient-to-r from-[#0FCFEC] to-[#19D3AE]">{children}</button>
    );
};

export default ButtonPrimary;